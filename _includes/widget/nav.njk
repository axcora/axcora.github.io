<div class="container">
<nav class="navbar row p-2">
<div class="col-md-3 col-3 p-1 mt-2"><strong>
<a class="navbar-brand p-3" href="{{metadata.url}}" title="{{metadata.title}}">{{metadata.navs}}</a>
</strong></div>
<div class="col-md-7 col-6">
<nav class="nav text-center row p-1">
{% for n in metadata.navbar %}<a class="col-4 nav-link" href="{{n.link}}" title="{{n.nav}}">{{n.nav}}</a>{% endfor %}
</nav>
</div>
<div class="col-md-2 col-2 p-1 text-end">
{% for flag in metadata.lang %}<a href="{{flag.link}}" title="{{flag.title}}">
<img class="img-fluid flag mt-2" width="20" height="20" src="{{flag.icon}}" alt="{{flag.title}}"/>
</a>
{% endfor %}
</div>
</nav>
</div>