---node
// <script>
const pagination = {
	data: "collections",
	size: 1,
	alias: "tag",
	filter: ["all", "posts"],
	addAllPagesToCollections: true,
};

const eleventyComputed = {
	title: "Tagged '{{ tag }}'",
	permalink: function(data) {
		return `/tags/${this.slugify(data.tag)}/`;
	}
};
---
<div class="container">
<div class="row mt-5">
<div class="col-md-10 mx-auto p-3 p-md-5">
<h1 class="title"><strong>{{ tag }}</strong></h1>
<h2 class="mt-3 lead">Explore all hastag for {{tag}}</h2>
</div>
</div>
</div>

<div class="container-fluid">
<div class="row mt-5">
<div class="col-md-10 mx-auto p-3 p-md-5">
{% set postslist = collections[ tag ] %}
{% include "postslist.njk" %}
<p class="text-center p-1 mt-5"><a href="tags.njk" class="btn btn-primary">Explore all hastags</a></p>
</div>
</div>
</div>