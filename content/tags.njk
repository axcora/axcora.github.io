---
title: Hastag Axcora
slogan: Hastag
description: List hastag axcora technology mesin kasir aplikasi kasir pembuatan website
---
<div class="container">
<div class="row mt-5">
<div class="col-md-10 mx-auto p-3 p-md-5">
<h1 class="lead col-md-10"><strong><a href="{{page.url}}" title="{{title}}"><small>{{title}}</small></a></strong></h1>
<h2 class="title col-md-10"><strong>{{slogan}}</strong></h2>
<p class="mt-3 col-md-10">{{description}}</p>
</div>
</div>
</div>
<div class="container">
<div class="col-md-10 mx-auto p-3">
<div class="row">
{% for tag in collections | getKeys | filterTagList %}
{% set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
<div class="col-md-3 col-6 p-1"><a class="btn-sm btn btn-dark col-12 p-1 mt-1 mb-1" href="{{ tagUrl }}" title="{{tag}}">#{{ tag }}</a></div>
{% endfor %}
</div>
</div>
</div>